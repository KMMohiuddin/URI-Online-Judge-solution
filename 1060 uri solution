#include<stdio.h>
int main()
{
    int i,a=0;
    float n;
    for(i=1;i<=6;i++)
        {
        scanf("%f",&n);
        if(n>0)
            a++;
        }
    printf("%d valores positivos\n",a);
    return 0;
}
